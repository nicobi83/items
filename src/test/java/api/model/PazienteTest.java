/*package api.model;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import static org.assertj.core.api.Assertions.assertThat;*/

/**
 * Created by NICOLA on 04/06/2016.
 */

/*public class PazienteTest {

    Logger logger = LoggerFactory.getLogger(this.getClass());

    Paziente paziente;
    Paziente paziente_test;

    @Before
    public void setUp() throws Exception {

        paziente = new Paziente();
        paziente_test = new Paziente();
        paziente.setCodiceFiscale("MRCBNC78L12H604M");
        paziente_test.setCodiceFiscale("BNCFER78L12H604M");
    }

    @After
    public void tearDown() throws Exception {

    }

    @Test
    public void testEquals() throws Exception {
        boolean should_be_different = paziente.equals(paziente_test);
        logger.info("Devono essere diversi: " + should_be_different);
        assertThat(should_be_different).isFalse();
        persona.setEmail(persona_test.getEmail());
        persona.setNome(persona_test.getNome());
        persona.setCognome(persona_test.getCognome());
        boolean should_be_same = persona.equals(persona_test);
        logger.info("Should be same: " + should_be_same);
        assertThat(should_be_same).isTrue();
    }

}*/
